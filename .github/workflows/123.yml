on:
  workflow_dispatch:

jobs:
  unit:
    runs-on: ubuntu
    steps:
    - run: echo test
  code:
    runs-on: ubuntu
    steps:
    - run: echo code
  docker:
    if: github.ref == 'refs/heads/feature/*'
    runs-on: ubuntu
    steps:
    - run: echo code
  dev:
    if: github.ref == 'refs/heads/feature/*'
    runs-on: ubuntu
    steps:
    - run: echo code
  dev-test:
    if: github.ref == 'refs/heads/feature/*'
    runs-on: ubuntu
    steps:
    - run: echo code
  prod:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu
    steps:
    - run: echo code
  prod-test:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu
    steps:
    - run: echo code
    
